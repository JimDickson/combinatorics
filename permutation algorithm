#algorithm to generate permutations

def print_permutations(n):
  
  #generate array of numbers
  n_array=[]
  x=1
  while x<n:
    n_array.append(n)
    x+=1
  
  print(n_array)
  
  #generate each permutation
  output_permuation=""
  
  #define function to add a number not used
  def next_not_used(permutation,numbers): 
    for x in array:
      while permutation.count(x)<1:
        permutation=permutation+x
  while x<n:
    if len(output_permutation)=n print(output_permutation) else:
      next_not_used(permutation,array)
      x+=1
  
